git tag
``` 
git tag -a v1.0 -m 'coommit'

git push origin --tags

git tag

git tag -l v1*

git show v1.0

git tag -d v1.0

git push origin :refs/tags/v1.0

git checkout v1.0

git pull origin --tags

git pull origin :remotes/origin/v2.1
```
